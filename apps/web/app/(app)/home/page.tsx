// Deliberately empty: ROADMAP.md's phase 0 acceptance criterion is "a new
// user can register, verify their email, log in, and see an empty
// authenticated screen." The timeline (fase 1) is what fills this in.
export default function HomePage() {
  return <h1 className="text-xl font-semibold">Inicio</h1>
}
