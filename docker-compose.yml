name: x

services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: x
      POSTGRES_PASSWORD: x
      POSTGRES_DB: x
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # Runs once against an empty data dir (the official image's own
      # /docker-entrypoint-initdb.d/ convention) — creates the role
      # postgres-replica below clones and streams from. See that service's
      # own comment for the full picture.
      - ./docker/postgres/init-replication.sh:/docker-entrypoint-initdb.d/init-replication.sh:ro
    # wal_level=logical (default is `replica`) — Debezium's Postgres
    # connector (ROADMAP.md 2.3, SPECS.md §10.2) reads change data through
    # logical decoding (the built-in pgoutput plugin), which doesn't exist
    # below this level. Also comfortably covers postgres-replica below:
    # `logical` is documented as a superset of the plain `replica` level
    # ordinary streaming (physical) replication needs, so nothing further
    # to raise here for it.
    command: ["postgres", "-c", "wal_level=logical"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U x"]
      interval: 5s
      timeout: 5s
      retries: 10

  # SPECS.md §14.2's "réplicas de lectura" — a real streaming (physical)
  # replica, not a mock: same postgres:17-alpine image as postgres above,
  # deliberately (production streaming replication requires matching
  # major.minor versions; two different images would risk silent version
  # skew this compose file can't catch). On first start against an empty
  # data dir it clones postgres above via a real `pg_basebackup -R` (which
  # also writes standby.signal + primary_conninfo itself — no separate
  # recovery config needed), then starts in standby mode and streams
  # continuously from then on; the mounted volume makes the basebackup
  # step a no-op on every later restart. Verified by hand before writing
  # this file: a row (and a column added via ALTER TABLE — physical
  # replication ships DDL too, not just DML) written on a from-scratch
  # primary was readable on the replica within a second, and the replica
  # correctly rejected a direct write of its own.
  #
  # packages/db's createReplicatedDatabase (apps/api's read-write-routing
  # plugin drives it per request) is what actually sends reads here by
  # default and to postgres above during the 5 s read-your-writes window
  # after a write — this service is only the physical target that split
  # reads from. Not the local dev default in .env.example: a single
  # developer never benefits from splitting read traffic, so DATABASE_URL
  # there stays pointed at postgres/pgbouncer above and
  # DATABASE_REPLICA_URLS stays unset (createReplicatedDatabase falls back
  # to primary-only reads when it is) — this only earns its keep once read
  # traffic is large enough to want scaling separately from writes, same
  # posture as pgbouncer above.
  postgres-replica:
    image: postgres:17-alpine
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGPASSWORD: x
    entrypoint: ["sh", "-c"]
    command:
      - |
        set -e
        if [ ! -s /var/lib/postgresql/data/PG_VERSION ]; then
          echo "cloning postgres via pg_basebackup..."
          pg_basebackup -h postgres -p 5432 -D /var/lib/postgresql/data -U replicator -Fp -Xs -R -v -P
          chmod 0700 /var/lib/postgresql/data
        fi
        exec docker-entrypoint.sh postgres
    ports:
      - "5434:5432"
    volumes:
      - postgres_replica_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U x"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s

  # SPECS.md §14.2: "PgBouncer en modo transaction, 100 conexiones por
  # instancia" — sits in front of postgres above, on its own port so
  # migrations/seeds (CREATE INDEX CONCURRENTLY, ALTER TYPE ... ADD VALUE —
  # session-level DDL transaction-mode pooling can't safely proxy) keep
  # using postgres:5432 directly while apps/api and apps/workers' own
  # runtime query traffic (their DATABASE_URL) points here instead. Not the
  # local dev default in .env.example — a single local Postgres never gets
  # near its own max_connections, so this only earns its keep once there's
  # more than one apps/api/apps/workers instance sharing the same database,
  # same posture as postgres-replica below.
  #
  # packages/db/src/client.ts's own `prepare: false` is what actually makes
  # pointing an app at this instead of postgres above safe: transaction-mode
  # pooling can hand two statements from the same "session" to two different
  # physical Postgres connections, which stale-invalidates postgres.js's
  # default auto-prepared statements. client.ts's own comment and
  # client.pgbouncer.integration.test.ts's top comment have the full,
  # empirically-verified story (it's a silent extra-round-trip tax under
  # real load, not a reliable hard crash — two separate safety nets already
  # paper over it, and prepare: false is what avoids depending on either).
  pgbouncer:
    image: edoburu/pgbouncer:latest
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://x:x@postgres:5432/x
      POOL_MODE: transaction
      MAX_CLIENT_CONN: "100"
      DEFAULT_POOL_SIZE: "20"
      # Not "trust": postgres above uses Postgres 17's default
      # password_encryption=scram-sha-256, and pg_hba.conf demands
      # scram-sha-256 for any non-local connection — including this
      # container's own backend leg to postgres. edoburu/pgbouncer's
      # entrypoint script only stores the *plaintext* DATABASE_URL password
      # (needed to complete a real SCRAM handshake against the backend)
      # when AUTH_TYPE is "plain" or "scram-sha-256"; anything else,
      # including "trust", makes it pre-hash the password to MD5 instead,
      # which cannot complete SCRAM — confirmed empirically (a manually
      # started container with AUTH_TYPE=trust fails every backend
      # connection with "cannot do SCRAM authentication: wrong password
      # type"; switching to scram-sha-256 fixed it, verified with 10
      # repeated queries through the proxy). This governs both legs:
      # clients connecting to this pgbouncer now also do real SCRAM (the
      # same x:x credentials already used everywhere else in this file).
      AUTH_TYPE: scram-sha-256
    ports:
      - "6432:5432"

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: x-minio
      MINIO_ROOT_PASSWORD: x-minio-secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 10

  mailpit:
    image: axllent/mailpit:latest
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI

  # ROADMAP.md 2.4 — analytics-only store for trend scoring (SPECS.md
  # §10.4). Nothing else in the product reads or writes ClickHouse; losing
  # it loses trending topics, never a post, a like, or a DM.
  clickhouse:
    image: clickhouse/clickhouse-server:24.8-alpine
    restart: unless-stopped
    environment:
      CLICKHOUSE_USER: x
      CLICKHOUSE_PASSWORD: x
      CLICKHOUSE_DB: x_analytics
    ports:
      - "8123:8123" # HTTP interface — what @clickhouse/client speaks
      - "9010:9000" # Native protocol, unused today but the image exposes it; host-mapped off ClickHouse's own default (9000) to not collide with MinIO's console port already at 9001 and Fastify apps commonly binding 900x in dev
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8123/ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ROADMAP.md 2.3 — SPECS.md §10.1/§10.2: full-text search + typeahead,
  # kept behind a single-node/no-security config since this is local dev,
  # never a production topology.
  opensearch:
    image: opensearchproject/opensearch:2
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - DISABLE_SECURITY_PLUGIN=true
      - DISABLE_INSTALL_DEMO_CONFIG=true
      # SPECS.md never asks for more than a laptop-scale local index —
      # OpenSearch's own JVM default (a fraction of host RAM) is tuned for
      # a dedicated search node, not one of six services sharing this box.
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - opensearch_data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20

  # Kafka-API-compatible, not literal Kafka — a single Redpanda binary needs
  # no separate Zookeeper (unlike a real Kafka broker), which matters here:
  # SPECS.md §10.2 only asks for a durable log between Debezium and the
  # indexer, not a specific implementation, and this is a local dev compose
  # file, never the production topology ROADMAP.md 3.1 migrates onto Kafka
  # proper. debezium/connect (below) speaks the Kafka wire protocol against
  # this exactly the same as it would against real Kafka.
  redpanda:
    image: redpandadata/redpanda:latest
    restart: unless-stopped
    command:
      - redpanda
      - start
      - --smp
      - "1"
      - --memory
      - "512M"
      - --overprovisioned
      - --node-id
      - "0"
      - --check=false
      # Two listeners on one binary, Redpanda's own documented way to serve
      # both a same-network container (debezium, below, reaching this one
      # by service name) and a host process (Testcontainers-driven tests,
      # or a developer's own machine) without them fighting over which
      # address gets advertised to Kafka clients after they connect.
      - --kafka-addr
      - PLAINTEXT://0.0.0.0:29092,OUTSIDE://0.0.0.0:9092
      - --advertise-kafka-addr
      - PLAINTEXT://redpanda:29092,OUTSIDE://localhost:9092
    ports:
      - "9092:9092"
    volumes:
      - redpanda_data:/var/lib/redpanda/data
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy:.*true' || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20

  # ROADMAP.md 2.3 — SPECS.md §10.2's "CDC con Debezium → Kafka": the
  # standard containerized path (Kafka Connect + the Debezium Postgres
  # connector bundled together), configured against redpanda above instead
  # of a real Kafka cluster — same reasoning redpanda's own comment gives.
  # Registering an actual connector (which tables, which topics) happens
  # via this container's REST API at runtime (scripts/register-cdc-connector.ts),
  # not baked into the image, so it can be re-run idempotently.
  debezium:
    image: debezium/connect:2.7.3.Final
    restart: unless-stopped
    depends_on:
      redpanda:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      BOOTSTRAP_SERVERS: redpanda:29092
      GROUP_ID: x-cdc
      # Debezium's own internal bookkeeping topics (connector offsets,
      # config, status) — distinct from the actual change-data topics a
      # connector config creates per table, never read by apps/workers.
      CONFIG_STORAGE_TOPIC: _x_connect_configs
      OFFSET_STORAGE_TOPIC: _x_connect_offsets
      STATUS_STORAGE_TOPIC: _x_connect_status
    ports:
      - "8083:8083" # Kafka Connect REST API — connector registration only, nothing in apps/* calls this at runtime
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8083/connectors || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30

  # ROADMAP.md 2.7 / SPECS.md §9.2's scan step. Ships with a signature DB
  # baked into the image, so clamd starts and scans real content even when
  # freshclam's own live update can't reach database.clamav.net (a
  # sandboxed/offline dev network) — confirmed empirically, not assumed.
  # The volume persists whatever freshclam *does* manage to fetch across
  # restarts, same reasoning as every other named volume here.
  clamav:
    image: clamav/clamav-debian:latest
    restart: unless-stopped
    ports:
      - "3310:3310" # clamd's own INSTREAM protocol port — apps/workers' clamav-client.ts speaks it directly, no CLI wrapper
    volumes:
      - clamav_data:/var/lib/clamav
    healthcheck:
      test: ["CMD-SHELL", "echo PING | nc -w 5 localhost 3310 | grep -q PONG"]
      interval: 10s
      timeout: 10s
      retries: 30
      start_period: 60s

volumes:
  postgres_data:
  postgres_replica_data:
  redis_data:
  minio_data:
  clickhouse_data:
  opensearch_data:
  redpanda_data:
  clamav_data:
